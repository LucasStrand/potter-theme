---
single: true
out: dist
filename: preset.cjs
---
// Potter — Tailwind v3 preset. AUTO-GENERATED by Quills.
// Requires @potternu/css loaded so the --potter-*-rgb vars exist.
//   const potter = require("@potternu/tailwind/dist/preset.cjs");
//   module.exports = { presets: [potter], ... };
module.exports = {
  theme: {
    extend: {
      colors: {
        potter: {
{{#each flavors}}{{#if default_light}}{{#each colorList}}          "{{name}}": "rgb(var(--potter-{{name}}-rgb) / <alpha-value>)",
{{/each}}{{/if}}{{/each}}        },
      },
    },
  },
};
